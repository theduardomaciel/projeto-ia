# Frontend public env vars (exposed to the browser)
# In dev, we recommend leaving this unset to use the Vite dev proxy (/api -> http://localhost:8000)
# PUBLIC_API_BASE_URL=/api
# For direct calls (no proxy), point to your backend URL, e.g.:
# PUBLIC_API_BASE_URL=http://localhost:8000/api

# Dev-only server-side env var to override Vite proxy target
# BACKEND_URL=http://localhost:8000
